# My LaTeX Document Template 

Replace `\authorName` and `\MatNr` variables in `chapters/config/00.1_config.tex`

# Compiling
Changes in the `.bib` file have to be included using **biber**
- compile once
- run biber `biber --output-directory ./out main`
- compile again
